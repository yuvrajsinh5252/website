# Local Environment Variables
# Copy this file to `.env` for local development:
# cp .env.example .env

# Optional developer feature flags:
# In development (`npm run dev`), dev tools (/logo-lab and palette preview) are active.
# In production (`npm run build`), they are automatically omitted from the bundle.
# VITE_DEV_TOOLS=true