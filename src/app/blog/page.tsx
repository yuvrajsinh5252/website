import Overlay from "@/components/overlay";

export default function Home() {
  return (
    <Overlay>
      <div className="group h-screen glow glow:ring-1 glow:border-glow glow:ring-glow glow:bg-glow/[.15]">
      </div>
    </Overlay>
  )
}