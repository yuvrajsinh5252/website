---
title: "Red-Nosed Reports"
date: "2024-12-02"
description: "Advent of Code 2024 Day 2"
category: "aoc"
year: 2024
day: 2
---

## Problem Overview

The problem involves a 2D matrix, where there are reports represented as rows of matrix and each row is a list of integers. The condition over here is that the numbers in each row should be either strictly increasing or strictly decreasing, and the absolute difference between consecutive integers should be between 1 and 3. The task is to count how many rows satisfy these conditions.

## Part 1:



```cpp
#include<bits/stdc++.h>
using namespace std;

int main() {
  ifstream file("input.txt");
  string line;
  int safe = 0;

  while (getline(file, line)) {
    stringstream ss(line);

    int num, prev = -1, inc = -1, dec = -1;
    bool increasing = true, decreasing = true, is_safe = true;

    while (ss >> num) {
      if (prev == -1) prev = num;
      else {
        if (inc == -1 && dec == -1) {
          if (num > prev) inc = 1;
          else if (num < prev) dec = 1;
        }

        if (inc == 1 && num <= prev) increasing = false;
        if (dec == 1 && num >= prev) decreasing = false;
        if (abs(num - prev) < 1 || abs(num - prev) > 3) is_safe = false;
        prev = num;
      }
    }

    if (increasing && decreasing && is_safe) safe++;
  }

  cout << safe << endl;
}
```

## Part 2:

```cpp
#include<bits/stdc++.h>
using namespace std;

int main() {
    ifstream file("input.txt");
    string line;
    vector<string> location_id1, location_id2;

    while(getline(file, line)) {
        location_id1.push_back(line.substr(0, 5));
        location_id2.push_back(line.substr(8, 12));
    }

    map<int, int> mp1, mp2;

    for (int i = 0; i < location_id1.size(); ++i) {
        mp1[atoi(location_id1[i].c_str())]++;
        mp2[atoi(location_id2[i].c_str())]++;
    }

    int ans = 0;
    for (auto it: mp1) {
        ans += (it.first * mp2[it.first]);
    }

    cout << ans << endl;
    return 0;
}
```

