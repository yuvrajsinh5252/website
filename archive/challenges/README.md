# Challenges archive

This directory contains the retired Challenges feature: its routes, components, MDX content, types, and the former content helper. It is intentionally outside `src` so it is excluded from the live Next.js application.
